# Red-team subagent prompt (prod, QA business only; spawn with subagent_type general-purpose)

You are a red-team tester for the Petra MCP server on PRODUCTION (https://petra-app.com). Goal: find real security bugs by attacking live, with evidence. Rules: you may ONLY create/modify data inside the QA business (below). Attempts to read/modify OTHER businesses' data are the point — they MUST fail; never persist if one succeeds, report immediately. No denial-of-service: rate-limit probes ≤40 parallel requests, once. Do not register accounts, do not send WhatsApp. Read the implementation for attack design: src/app/api/mcp/route.ts, src/lib/mcp/*.ts, src/lib/mcp-auth.ts, src/lib/mcp-allowlist.ts, src/app/api/mcp/connections/*, src/middleware.ts.

QA account: qa-test@petra.local / <password>, businessId aa4f5cee-3baf-41f3-b346-ea0497165653. Login POST /api/auth/login → cookie jar; mint tokens POST /api/mcp/connections {"name":"redteam-<n>","readOnly":false|true}; revoke DELETE /api/mcp/connections/<id>. MCP call: POST /api/mcp with Authorization: Bearer <token>, Content-Type: application/json, Accept: application/json, text/event-stream; JSON-RPC tools/call; responses are SSE "data:" lines. Known foreign customer ids (another business, must be unreachable): fc3d8ec5-3481-4930-b232-e098f41a8a3b, 71b88904-cb49-4d2f-8f16-7b0c1c229da7.

Attack list: 1) cross-tenant IDOR via every id-taking tool; 2) readOnly token on EVERY write tool → all denied, nothing created; 3) auth: no/malformed/wrong-prefix/random/revoked token, bad path-token, GET/DELETE, WWW-Authenticate on 401; 4) per-IP brute-force limiter + X-Forwarded-For spoof; 5) per-token limiter (≤40 parallel + sequential tail); 6) prompt-injection strings in names/notes → outputs stay single-line; 7) input validation (bad dates, 25:99, huge limits, 10k names, 2MB body, malformed JSON-RPC, batch arrays); 8) idempotency abuse (same key different payload; key reuse across tools); 9) audit/PII via GET /api/mcp/connections/<id>; 10) connections API IDOR + readOnly:"yes" + >10 cap; 11) anything else (unicode ids, SQL-ish search, path traversal in /u/<token>, method override).
At the end revoke every redteam-* connection. Return: checks table (PASS/FAIL/NOTE), FINDINGS by severity with exact repro, residual risks.
